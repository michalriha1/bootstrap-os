#################
### AUTOSTART ###
#################

# GUI SUDO
exec-once = systemctl --user start hyprpolkitagent

# controll bar
exec-once = qs -c noctalia-shell

# idle daemon
exec-once = hypridle

# Openclaw node tunnel
exec-once = autossh -M 0 -N -o "ServerAliveInterval=30" -L 18789:127.0.0.1:18789 michal@192.168.0.105

